import type { Metadata } from 'next';
import './globals.css';
import Navbar from '@/components/Navbar';
import Footer from '@/components/Footer';

export const metadata: Metadata = {
  title: 'Khojbeen | AI-Powered Smart Campus Lost & Found Assistant',
  description: 'Smart lost and found assistant powered by Voice AI, Multimodal Photo Matching, and Privacy Secrecy Shield.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
